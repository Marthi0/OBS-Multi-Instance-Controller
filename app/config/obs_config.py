"""Configure OBS via command-line arguments.

In OBS 32.x+, WebSocket can be configured directly via CLI flags:
  --websocket_port <port>
  --websocket_password <password>

This module is kept for reference but the actual configuration
is now done in obs_launcher.py via command-line arguments.
"""
